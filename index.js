var player =0;
var move = 0;

document.querySelectorAll(".cells").addEventListener("click", playMove(move));
document.querySelectorAll(".cells").addEventListener("click", playMove(move));

function playMove(move){
    for(var i =1;i<=9;i++){
        console.log(document.querySelectorAll(".${i}"));
    }
    
}